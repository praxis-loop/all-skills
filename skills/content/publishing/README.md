# content/publishing

`content/publishing` 用于把已定稿内容投递到具体发布平台。

适合放入本子类的 skill：

- 平台排版：把定稿内容转成平台要求的富文本或结构。
- 草稿投递：写入平台草稿箱并回读核对。
- 发布前的授权确认和重复草稿防护。

边界：本子类的 skill 默认只写草稿，不执行公开发布或群发。任何公开动作必须先向用户确认。

新增 skill 时使用：`skills/content/publishing/<skill-name>/SKILL.md`。
