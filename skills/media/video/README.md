# media/video

`media/video` 用于视频的生成、剪辑说明和成片装配。

适合放入本子类的 skill：

- 由文稿或 beat map 驱动的短片生成。
- 旁白 TTS、字幕、变速和拼装。
- 成片质量检查。

依赖提醒：本子类的 skill 通常需要 `ffmpeg`/`ffprobe` 和外部生成服务，安装前先确认运行环境。

新增 skill 时使用：`skills/media/video/<skill-name>/SKILL.md`。
